<div class="container">
    <h1>Список разрешений</h1>
    <div class="row">
        {{#if error}}
            <font color='red'>{{error}}</font>
        {{/if}}
        {{#each permissions}}
        <div class="col-lg-6">
            <ul class="list-group">
                <li class="list-group-item">
                    {{this.permission}}
                </li>
            </ul>
        </div>
        <div class="col-lg-2">
            <ul class="list-group">
                <form action="/rules/deletePermission/{{this.id}}" method="post">
                    <input type="submit" class="btn btn-danger" value="Удалить разрешение">
                </form>
            </ul>
        </div>
        {{/each}}
    </div>
</div>
