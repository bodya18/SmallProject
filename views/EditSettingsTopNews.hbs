{{!-- <a class="btn btn-dark settings-rmChoice" href="/news/GetEditSettings/{{settings._key}}">Убрать выбор</a> --}}
<div class="container">
    <div class="row">
        <div class="col-lg-12">
            
            <form action="/news/settings/serch" method="POST">
                <input type="hidden" value="{{settings._key}}" name="key">
                <div class="form-group">
                    <br>
                    <select class="js-example-basic-multiple selectCategory-settings" multiple="multiple" name="SelectCategory" data-placeholder="Выберите категории показа" >
                        {{#each categoriesSelect}}
                            <option value="{{this.id}}">{{this.category}}</option>
                        {{/each}}
                    </select>
                </div>
                <div class="col-lg-6">
                    <input type="search" name="search" class="form-control search-settings-inp" placeholder="Выберите новость" aria-label="Search" />
                </div>
                <button type="submit" class="btn btn-lg btn-default search-settings-btn">
                    <i class="fa fa-search"></i>
                </button><BR>
                
            </form>

            <form method="POST" action="/news/editSettings">
                {{#if error}}
                    <font color='red'>{{error}}</font>
                {{/if}}<br>
                <h1>{{settings.label}}</h1>
                <input type="submit" class="btn btn-success set-setting" value="ЗАДАТЬ НАСТРОЙКУ" >
                <input type="hidden" value="{{settings.label}}" name="label">
                <input type="hidden" value="{{settings.id}}" name="id">
                <input type="hidden" value="{{settings._key}}" name="key">

                
                <div class="cat-news">
                    <div class="col-md-12">
                        <div class="row">
                            {{#each @root.news}}
                                {{#if_eq this.time @root.nowDate}}
                                    <div class="col-md-3">
                                        <div class="cn-img">
                                            <img class="show-Posts" src="/{{this.postUrl}}" />
                                            <div class="cn-title">
                                                <a class="none-decloration" href="/news/get/{{this.id}}">{{this.title}}</a>
                                                <input class="form-check-input" type="checkbox" value="{{this.id}}" name="checkNews">
                                            </div>
                                        </div>
                                    </div>
                                {{/if_eq}}
                            {{/each}}
                        </div>
                    </div>
                </div> 
            </form>
        </div>
    </div>
</div>